
import os
from google import generativeai as genai

# Setup API Key (using placeholder as I don't have the user's key in environment)
# But I can use the tool to run a list if I had a key.
# Alternatively, I can just assume v1beta is needed.

# Let's try to find documentation on the exact URL.
